spring:
  application:
    name: user-account-service

  datasource:
    url: jdbc:postgresql://localhost:5432/buuchezobank_users_account_db
    username: postgres
    password: Kadinyah02+++

  jpa:
    hibernate:
      ddl-auto: update

  # Kafka
  kafka:
    bootstrap-servers: localhost:9092

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type: true

server:
  port: 8080

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"

# Eureka
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

jwt:
  secret: "mffdgldfjldhbhfd--gsglsddlwefkgmghb-fsfgwsgwef-gerggrhrehwgsgsgwewe-ggrgwergwergewrgeg-"
  expiration: 86400000 #1day